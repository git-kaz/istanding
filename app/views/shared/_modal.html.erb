<div class="fixed inset-0 bg-background-dark/70 backdrop-blur-sm flex items-center justify-center z-50">
    <div class="bg-input-bg-light border border-border-light shadow-xl rounded-3xl max-w-sm w-full p-6 animate-[fadeIn_.2s_ease-out]">
        <h3 class="text-lg font-bold text-text-main">お疲れ様でした！</h3>

        <p class="mt-2 text-text-secondary">
            おすすめの運動：
            <strong class="text-text-main"><%= exercise.name %></strong>
        </p>

        <div class="mt-4">

            <% if exercise.cloudinary_public_id.present? %>
                <%= cl_image_tag(
                    exercise.cloudinary_public_id,
                    quality: "auto:best",
                    class: "rounded-xl border border-border-light"
                ) %>
            <% end %>

            <p class="text-sm text-text-secondary mt-3 leading-relaxed">
                <%= exercise.description %>
            </p>
        </div>

        <button
            class="mt-6 w-full py-3 rounded-2xl font-medium bg-primary text-white hover:bg-primary-hover transition active:scale-95"
            onClick="this.closest('#modal_container').innerHTML=''">
            閉じる
        </button>

    </div>
</div>